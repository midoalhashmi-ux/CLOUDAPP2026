# قواعد حماية بنية المشروع

هذا الملف مرجعي فقط — يحدّد الملفات والقرارات "الحساسة" التي **لا تُعدَّل إلا
بإذن صريح من صاحب المشروع**، لأن أي خطأ فيها يعطّل التطبيق بالكامل (وليس ميزة
واحدة فقط)، أو يكسر التوافق بين تطبيق المحتوى وتطبيق المشغل.

## 1. ملفات لا تُعدَّل إلا بإذن صريح

| الملف | سبب الحساسية |
|---|---|
| `lib/main.dart` | نقطة إقلاع التطبيق، تهيئة Firebase، شجرة التنقل الجذرية (Shell) |
| `lib/firebase_options.dart` | إعدادات مشروع Firebase — أي خطأ يمنع التطبيق من العمل بالكامل |
| `lib/core/services/player_launcher.dart` | جسر الاتصال بتطبيق المشغل (اسم الحزمة + رابط عميق) — أي تغيير هنا لازم يُطابقه تغيير مقابل في المشغل، وإلا توقف تشغيل القنوات بالكامل |
| `lib/core/services/secure_stream_service.dart` | آلية حماية روابط البث |
| `firestore.rules` | قواعد أمان قاعدة البيانات — خطأ هنا قد يفتح البيانات للجميع أو يقفلها بالكامل |
| `functions/index.js` | Cloud Functions (منها ما يخص روابط البث المؤقتة) |
| `android/app/build.gradle`, `codemagic.yaml` | إعدادات البناء والنشر |

## 2. قرارات ثابتة (لا تُغيَّر دون نقاش)

- **رابط الربط بين التطبيقين**: تطبيق المحتوى يرسل `channelId` فقط (لا يملك رابط
  البث أبداً) عبر Intent صريح لاسم حزمة المشغل، مع رابط عميق داخلي كخطة بديلة.
  هذا موثّق في `player_launcher.dart` والمشغل يتوقعه بنفس الشكل في `main.dart`
  الخاص به (`channelId` / `url` / `type`).
- **اللغة**: عربي فقط، بدون تعدد لغات.
- **الإعدادات القابلة للتغيير أونلاين تُقرأ من Firestore فقط** (`settings/*`
  في الغالب) — لا تُثبَّت قيم حساسة (ألوان، نصوص، تفعيل الإعلانات، حالة
  الصيانة...) داخل الكود مباشرة.
- **مصدر بيانات المباريات**: TheSportsDB (مجاني، بدون مفتاح خاص) عبر
  `matches_service.dart`. أي استبدال لمصدر آخر يتطلب تعديل هذا الملف فقط
  (باقي التطبيق يتعامل مع `MatchModel` الموحّد، لا مع استجابة المصدر مباشرة).
- **ترجمة أسماء الفرق/الدوريات**: قاموس محلي في `football_ar_translations.dart`.
  أي اسم غير موجود في القاموس يُعرض كما ورد من المصدر (إنجليزي) بدل ما يُخفى —
  فلا تحذف هذا السلوك الاحتياطي.

## 3. أسلوب العمل مع Claude في هذا المشروع

- أي تعديل على ملف من جدول القسم 1 يتطلب من Claude طلب إذن صريح قبل التنفيذ،
  وشرح سبب الحاجة للتعديل باختصار.
- التعديلات على باقي الملفات (شاشات، خدمات فرعية، نماذج بيانات) لا تحتاج إذناً
  مسبقاً، لكن يجب أن تبقى متوافقة مع القرارات الثابتة في القسم 2.
- عند إضافة ميزة جديدة: تُبنى كملفات جديدة قدر الإمكان (خدمة/شاشة مستقلة) بدل
  حشر منطقها داخل ملفات موجودة كبيرة، حفاظاً على البنية "صغيرة" وسهلة المراجعة.
